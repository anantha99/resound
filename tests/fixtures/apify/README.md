# Apify fixture state

`manifest.json` intentionally leaves all fourteen required live path captures unapproved. No
actor Run was made for Task 2, so the manifest contains no invented fixture hash or canary proof.

Files below `synthetic/` are hand-authored parser unit inputs only. They are not provider captures,
formal schemas, canary evidence, or approval evidence and must never be referenced as such.

# Resound — Design Decisions

Locked design decisions, organized by feature area. Each entry cites the task it was decided in and the reasoning, so future tasks can tell whether a decision still applies or needs to be re-litigated.

When a decision changes, **update this file in the same PR as the code change** and add a note under "Superseded decisions" at the bottom.

---

## Task 1 — LLM Gateway Abstraction

Locked in grilling session on **2026-05-02**. Source task: `task-master show 1`.

### Architecture

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 1 | **Per-brand gateway instance.** One `OpenRouterGateway` per `Pipeline`, constructed at brand load. | Mirrors the existing per-brand pattern for `Classifier`/`Router`/`Memory` in `Pipeline.__init__`. OpenAI client is cheap to instantiate. | Process-level singleton with `{brand_slug: ModelsConfig}` registry — would force every call site to thread `brand_slug`, and `stage` alone would no longer fully resolve the model. |
| 2 | **`response_schema` is flag-only.** `dict \| None` sentinel meaning "request JSON mode." Gateway does NOT validate the response against the schema. | Matches what `classifiers/openrouter.py` does today. Callers already have Pydantic models — second validation layer would be duplicate work that can disagree. OpenRouter's strict-JSON-schema mode coverage is patchy across models. | Pass-through JSON Schema with gateway validation; typed parse with `response_model: type[BaseModel]`. Both can be added later without breaking callers. |
| 3 | **Retry budget is fresh per fallback model.** Per-model: 3 attempts with exp backoff (2/4/8s) on transient errors (5xx/429/timeout). Each fallback model gets its own fresh 3-attempt budget. | Point of fallbacks is to escape an outage on the *primary provider* — sharing the budget defeats that. Bounded by the per-stage `timeout_s`. | Shared budget across the chain. |
| 4 | **Per-stage wall-clock cap (`timeout_s`).** Hard ceiling per `complete()` call; raises `LLMGatewayTimeoutError` even if retries/fallbacks remain. Filter ~5s, classify ~30s, others ~10s. | Without this, one slow signal can stall the pipeline for ~45s+ (3 retries × backoff × multiple fallbacks). Per-stage rather than global because filter ≠ classify in latency budget. | Single global timeout. |
| 5 | **Fallback trigger is narrow.** Only 404 / 422 / 413 / OpenRouter "no available provider" trigger fallback. 400 / 401 / 403 are **fatal** — raise immediately. | Wasting 3 round trips to confirm the API key is still bad helps no one. 400/401/403 are bugs to fix, not to fall back from. | Plan's original "any 4xx (non-429)" — too broad. |
| 6 | **JSON mode: try-and-detect with per-process cache.** First call uses native JSON mode; on 400 with `response_format`/`json_object` in the error, mark `model → no_json_mode` in an instance-level set, retry once with prompt suffix + regex extraction. **Always** run regex extraction defensively (even on JSON-mode success). | Hardcoded blocklists rot fast (OpenRouter changes weekly); operators shouldn't have to know which models support JSON mode. One wasted request per cold-start model is acceptable. | Hardcoded blocklist; hardcoded allowlist; per-model annotation in `models.yaml`; never use native JSON mode. |
| 7 | **Cost from OpenRouter only — no price table for demo.** Always include `extra_body={"usage": {"include": true}}`. If `response.usage.cost` present → use it. If missing → `cost_usd = None` + warning. No hardcoded prices, no `/models` fetch. | Demo scope cut. Live price tables rot fast and aren't worth implementing for a one-shot demo. Revisit post-demo if the audit trail in Task 3 needs cost completeness. | Hardcoded `MODEL_PRICES` dict; YAML-defined prices; `GET /models` fetch at startup; per-call `GET /generation/{id}`. |

### API surface

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 8 | **No `model_override` on `complete()`.** CLI testing (Task 10) builds a one-off gateway with custom config. | Demo scope. The override path can be added in one line later without breaking existing callers. | `complete(..., model_override: str \| None)` with override skipping fallbacks. |
| 9 | **No per-call `temperature` / `max_tokens`.** `StageConfig` is the single source of truth, read by the gateway per call. | Plan had function-default `0.1` / `1024` AND fields in `StageConfig` — the function defaults would silently override YAML. Cleaner to drop them entirely. | Keep them as `None`-defaulted overrides falling through to `StageConfig`. |
| 10 | **Final `complete()` signature**: `complete(stage: str, prompt: str, response_schema: dict \| None = None) -> LLMResponse`. | Smallest surface that does the job. Adding more knobs is one-line work later. | Original plan signature with temperature, max_tokens, response_schema, etc. |
| 11 | **`LLMResponse` stays lean — 9 fields**: `content`, `model_used`, `tokens_in`, `tokens_out`, `cost_usd: float \| None`, `latency_ms`, `raw_response`, `was_fallback: bool` (added per #29), `attempt_count: int` (added per #29). | `stage` is redundant (caller passed it). `attempts` (per-attempt history list) is captured by `LLMGatewayExhaustedError` on failure path. `prompt_hash`/`timestamp` belong to the audit trail layer (Task 3), not the gateway. `raw_response` is the escape hatch. **Amendment 2026-05-05**: `was_fallback` and `attempt_count` are *aggregate* flags (not per-attempt arrays), needed by Task 3's audit trail to compute fallback rate without joining against current `ModelsConfig`. They stay within the spirit of "lean" — two scalars, no nested structure. | Adding `stage`, `attempts` (per-attempt array), `prompt_hash`, `timestamp` directly on success-path responses. |
| 12 | **`stage: str`** (plain string, not `Literal`). Unknown stage → `LLMGatewayConfigError` at lookup time. | Matches existing taskmaster note (2026-05-02): allows extensibility without code changes. | `Literal['filter' \| 'classify' \| 'routing_tiebreaker' \| 'memory_query']`. |

### Errors

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 13 | **Hierarchical exception tree, 5 leaf classes** under `LLMGatewayError`: `LLMGatewayConfigError`, `LLMGatewayAuthError`, `LLMGatewayExhaustedError(attempts=...)`, `LLMGatewayTimeoutError`, `LLMGatewayParseError(raw_text=...)`. | Hierarchy lets Pipeline catch `LLMGatewayError` broadly (skip a signal, move on) while letting Config/Auth errors bubble out and crash the worker so the operator notices. | Single `LLMGatewayError` (loses recoverability info); flat sibling classes (broad-catch becomes painful). |
| 14 | **Config/Auth errors are FATAL** — Pipeline does not catch them. Exhausted/Timeout/Parse errors are caught broadly via `LLMGatewayError`, signal marked `errored`, pipeline continues. | If `models.yaml` is malformed or `OPENROUTER_API_KEY` is wrong, the worker should die loudly at startup, not silently skip every signal. | Catch-all error handling. |

### Config

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 15 | **`StageConfig` is a Pydantic `BaseModel`** with exactly 5 fields: `model`, `temperature`, `max_tokens`, `fallbacks`, `timeout_s`. | Pydantic gives free YAML-load validation — type errors at startup, not at runtime. `timeout_s` was added during grilling (Q3) and is per-stage. | Plain `dataclass` (no validation); adding `prompt_price` / `provider_routing` / `system_prompt` (out of demo scope). |
| 16 | **Brand override merge is field-level** (over global defaults). Brand override file specifies only deltas; missing fields inherit. **List fields (`fallbacks`) replace whole** — do not concat. | DRY brand files. List concat for fallback chains is rarely what callers want — order matters and you usually want to *replace* the chain for a brand. | Stage-level replacement (verbose); list concat. |
| 17 | **Default models in `config/models.yaml` (subtask 1.6) are DEMO-PROVISIONAL.** Ship reasonable defaults now (`claude-sonnet-4-6` for classify, `gpt-4.1-mini` elsewhere, with 1-element fallbacks for *all* stages). **Revisit at demo time** with current top-tier efficient+cheap models. | User flagged in grilling: model availability/quality on OpenRouter churns fast. A model picked weeks before a demo may be superseded. See `feedback_demo_model_selection.md` in user memory. | Lock in models now and forget about them. |

### Wiring

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 18 | **`OpenRouterGateway` constructor**: `(config, api_key=None, http_referer=None, app_title=None, client=None)` — env defaults for the optionals, `client` injectable for tests. | `client` injection makes every retry/fallback test 5 lines instead of 50 (no monkeypatching `openai.OpenAI`). Env defaults match the existing classifier pattern. | Constructor takes `ModelsConfig` only (testability tax). |
| 19 | **`build_gateway(brand_slug)` is the public one-liner**: returns `OpenRouterGateway(config=load_models_config(brand_slug))`. The explicit `OpenRouterGateway(config=..., client=...)` form remains for tests. | Public API stays brand-shaped; tests can bypass it. | Factory takes `ModelsConfig` (verbose for callers); only the constructor (no factory). |
| 20 | **Pipeline wiring is DEFERRED to Task 9.** Task 1 ships the gateway module standalone with isolated tests. **Do NOT modify `src/resound/pipeline.py`** in Task 1. | Keeps Task 1's diff small and reviewable. Gateway is testable in isolation; integration follows naturally when classifiers refactor onto it. | Wire the gateway into `Pipeline.__init__` immediately and create unused parameters until Task 9. |

---

---

## Task 2 — models.yaml Brand Integration

Locked in grilling session on **2026-05-03**. Source task: `task-master show 2`.

**Context:** Task 2 was originally scoped to also create `config/models.yaml` and the loader/merge logic. Those got absorbed into Task 1 (subtasks 1.2 and 1.6), so Task 2 was rescoped here to integration-only — the brand-side artifacts and developer-facing comms.

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 21 | **Task 2 rescoped to integration-only.** Three subtasks: (2.1) example brand override file, (2.2) `BrandConfig` docstring, (2.3) `.env.example` deprecation. The schema, loader, global defaults, and merge semantics stay owned by Task 1. | Subtasks 1.2 and 1.6 already do everything Task 2 originally proposed for the schema/loader/defaults. Rescoping prevents duplicate work and conflicts with locked Task 1 decisions (#15 `timeout_s`, #16 merge semantics). Dependency `["1"]` already orders this correctly: Task 2 = "now wire it into the brand pathway." | (A) Delete Task 2, fold remaining work into Task 1 (buries the brand-integration story). (C) Keep Task 2 as-is and remove subtasks 1.2/1.6 (would unwind locked Task 1 decisions). |
| 22 | **`brands/liquiddeath/models.yaml` is a single-field override of `classify.model`** (demo-provisional model name; revisit per #17). Inline comment explains *why* this brand is the example: loud/sarcastic voice → default classify model misreads ironic praise as genuine and snark as outage-level → stronger model improves precision on `action_class`. | This file is the on-camera artifact for the demo's "model swap" moment (Tasks 21, 25). One overridden field reads more cleanly on screen than multi-stage overrides; the field-level merge point lands harder when there's literally one line of override against a dozen lines of inherited config. Smarter-model narrative ("match model intelligence to brand difficulty") is a stronger product story than "we made it cheaper." | Multi-stage override (more to read on screen); whole-stage override demonstrating list-replacement (correct but invisible to a non-code-reading reviewer); cheaper-model override (weaker narrative). |
| 23 | **No file for `brands/fulfil/`** — `brands/fulfil/models.yaml` does not exist. | The absence itself demonstrates that brand overrides are opt-in. A reviewer scanning the diff sees one brand with the file, one without, and immediately understands the convention without having to open and read an empty file. YAGNI: don't pre-create artifacts for hypothetical needs (same principle as #20). | Empty file with header comment (busywork — adds noise without informational value); different override for fulfil to contrast cost vs quality (splits demo attention). |
| 24 | **`BrandConfig` gets a docstring update only — NO data field changes.** Add to the class docstring: `brands/<slug>/models.yaml` exists and is loaded by `resound.gateway.load_models_config(slug)`; explain that it's deliberately not exposed as a `BrandConfig` field. | No current caller (Pipeline post-Task-9, CLI testing in Task 10) reads from `brand.models`. Adding the field would create a `resound.config → resound.gateway` import edge that doesn't otherwise exist. A merged `ModelsConfig` would be inconsistent with the raw-dict shape of `routing` / `people` / `views`. Same principle as #20 (deferred wiring) — don't pre-wire things no caller needs. | (A) `models: dict[str, Any]` raw field on `BrandConfig` (dead weight; one more loaded-but-unread field). (B) `models: ModelsConfig` (inconsistent shape; new import edge). |
| 25 | **`.env.example` surgical edits only**: (a) **delete** `RESOUND_FILTER_MODEL` line outright (was documented but never read by any code), (b) **comment out** `RESOUND_CLASSIFIER_MODEL` with a `# DEPRECATED — ...` note pointing at `config/models.yaml`, (c) **leave `RESOUND_CLASSIFIER_PROVIDER` untouched** (still load-bearing for the factory switch; Task 9 removes both the var and the factory in one motion). | RESOUND_FILTER_MODEL was pure cruft — labeling it deprecated would be dishonest about what was ever live. RESOUND_CLASSIFIER_MODEL is genuinely live until Task 9 refactors classifiers onto the gateway, so commenting (not deleting) preserves discoverability. Deprecating RESOUND_CLASSIFIER_PROVIDER now would announce a deprecation that doesn't bite for several tasks — Task 9 should kill the var and the factory together for a clean removal. | (a) Hard-remove both env vars now and update existing classifiers to read `models.yaml` (premature; bloats Task 2 and steps on Task 9's refactor). (c) Don't touch `.env.example` at all (a developer cloning between Tasks 2 and 9 wouldn't know `models.yaml` is the new way). |

---

## Task 3 — `llm_calls` Audit Trail

Locked in grilling session on **2026-05-05**. Source task: `task-master show 3`.

**Context:** Task 3 ships the audit-trail schema, writer methods, and query methods for LLM telemetry (cost, latency, fallback rate). The wiring into `Pipeline` lands in Task 9 (mirrors Task 1 #20). The original plan included Alembic migration scaffolding; that was rejected as disproportionate to demo scope.

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 26 | **Skip Alembic for Task 3.** `Base.metadata.create_all()` (already used at `src/resound/memory/__init__.py:118`) creates the new `llm_calls` table on next instantiation. Document in README that schema management is `create_all`-driven for demo simplicity; production deployment would introduce Alembic in a single follow-up task that backfills initial migrations for all 5 tables together. | No live data to migrate — SQLite file gets blown away between demo runs. Alembic adoption requires generating an initial migration for the *existing* 4 tables (signals, classifications, routes, feedback_events) plus the new one — disproportionate setup cost for demo scope. | Add Alembic per the original plan (multi-day setup overhead); hand-rolled `.sql` migration files (halfway compromise that does no one any favors). |
| 27 | **`LLMCallRow.signal_id` is a nullable FK to `SignalRow`,** indexed. Per-signal stages (`filter`/`classify`/`routing_tiebreaker`) populate it; `memory_query` (per Task 6) leaves it null because it's a meta-level operation against the corpus, not tied to one signal. **No cascade** on delete — the audit trail survives signal deletion. | Enables a single `JOIN llm_calls ON signal_id` for "show me all LLM activity for signal X" — the natural debugging query. Nullable handles `memory_query` cleanly without forcing a synthetic "system" signal. Matches existing FK pattern (`ClassificationRow.signal_id`, `RouteRow.signal_id`). | Standalone (no FK) — would force fuzzy `prompt_hash` matches for the most common debugging query. Required FK — would break `memory_query`. |
| 28 | **Two writer methods, not one**: `record_llm_call(...)` for success path, `record_llm_failure(...)` for `LLMGatewayError` paths. Both use keyword-only args after the leading positional. | Failure-path callers have no `LLMResponse` to pass — making it `Optional[LLMResponse]` with conditional logic everywhere is uglier than two clean methods. Forces nullable schema fields where failure rows have nothing meaningful (`model`, `tokens_in`, `tokens_out`, `cost_usd`, `response_content`). | Single method with `success: bool` flag and optional response (forces conditional branches throughout the writer body). |
| 29 | **`LLMResponse` gains `was_fallback: bool` (default False) and `attempt_count: int` (default 1).** This is a **2-field amendment to Task 1 #11.** The `OpenRouterGateway.complete()` implementation sets these based on what actually happened during the call orchestration. | Without these flags, computing `query_fallback_rate` requires joining against current `ModelsConfig.StageConfig.model` to detect "this row's model isn't the primary" — lossy if config changes after the call. Storing the flags at write time is authoritative and cheap. The fields are aggregate scalars, not per-attempt arrays — they don't violate the spirit of #11 (which excluded per-attempt history). | Lossy "compare to current config" approach (drifts when config changes); exposing per-attempt history on `LLMResponse` (would substantively reopen #11). |
| 30 | **Schema text storage**: keep `prompt_hash` (SHA-256, indexed — for "are we deduping" metric); **drop `response_hash`** (no real use case — responses aren't deduped, prompts are); **add `response_content: Text \| None`** (verbatim `LLMResponse.content` — the only artifact you can't reconstruct from elsewhere); **skip storing the assembled prompt text** (signal content is in `signals.content`, system templates are static code); **skip `raw_response` dict storage** (heavy; demo doesn't need full forensics — add later if production wants it). | `response_content` is the highest-value forensic field per byte stored. Skipping prompt text avoids duplicating user-generated content into a second table (PII deletion footgun). Skipping `raw_response` avoids JSON-blob bloat for cases that almost never come up at demo scale. | Hash-only (loses forensic value); full prompt+raw_response storage (PII concerns + storage bloat). |
| 31 | **Task 3 ships writers + queries unused; Task 9 wires the Pipeline.** Mirrors Task 1 #20 — one integration moment, one PR, no churn. Task 3's PR test surface is "instantiate `SqlMemory`, call writers, query back, assert" — no gateway/pipeline mocks needed. | Wiring in Task 3 would force a partial classifier refactor (current classifier returns `Classification`, not `LLMResponse`) that Task 9 will redo. Reviewers see the Pipeline diff once, in Task 9, when both the gateway and the audit layer wire in together. | Wire Pipeline in Task 3 (causes Pipeline diff churn between Task 3 and Task 9); ship a wrapper/decorator (over-engineering for what is six lines per stage in Task 9). |
| 32 | **All percentile computation is Python-side** — `query_llm_latency` materializes per-stage `latency_ms` rows, sorts in Python, slices p50/p95/p99 indices. Exclude `success=False` rows from latency stats. | SQLite has no native percentile function and SQLite is the demo default. Demo data fits trivially in memory (a few thousand rows max). Cross-DB portable; honors the "SQLite for dev, Postgres for prod, same code" promise at the top of `memory/__init__.py`. Excluding failures prevents timeout-capped 30s outliers from skewing p95. Document the production swap (dialect-aware `percentile_cont`) in the method docstring. | Dialect-aware percentile_cont (breaks portability for negligible benefit at demo scale); SQLite UDF (per-connection registration noise). |
| 33 | **`since: datetime` is required on every query method** — no defaults like "last 24h". | Ambiguous defaults bite when dashboards render empty because nothing happened in the implicit window. Caller passes the explicit datetime. | Default `since=datetime.utcnow() - timedelta(days=1)` (silent emptiness when the implicit window doesn't match what the caller assumed). |
| 34 | **No time-bucketed cost queries in Task 3** (e.g., `query_llm_costs_by_day`). If Task 7's dashboard needs sparklines, add then. | Don't speculate about what the dashboard will render until Task 7 is being built. Adding speculative methods adds test surface and review cost for no current consumer. | Pre-build `query_llm_costs_by_day` / `query_llm_costs_by_hour` (YAGNI). |

---

## Task 9 — Refactor Classifiers to Use Gateway

Locked in grilling session on **2026-05-07**. Source task: `task-master show 9`.

**Context:** Task 9 is the wiring step that activates the infrastructure shipped in Tasks 1–3. Pre-Task 9 there are two parallel OpenRouter integrations: `OpenRouterGateway` (production-hardened, unused by production code paths) and `OpenRouterClassifier` (currently used, no retry/fallback, no audit). Task 9 collapses these into one path, populates `llm_calls` rows during real runs, and activates per-brand `models.yaml` overrides for the classify stage. Tasks 4–8 are explicitly out of scope per the user's reduced-demo plan (`PRD-demo.md` §5.2 already deferred them); their gateway-consuming integrations stay deferred. Task 9 ships with **one** gateway consumer (the classifier) and explicit acknowledgment that future stages will need their own factory wiring when they land.

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 35 | **Delete `ClaudeClassifier` outright.** Drop the file, the `anthropic` SDK dependency, the `RESOUND_CLASSIFIER_PROVIDER` factory toggle. OpenRouter is the only LLM path. Anthropic models reachable via OpenRouter slugs (`anthropic/claude-sonnet-4-6`). | The gateway IS the multi-provider abstraction (OpenRouter routes to 200+ models). A separate direct-Anthropic classifier duplicates that capability with worse hardening (no retry, no fallback chain, no audit) and is the source of the dual-implementation smell Task 9 exists to remove. | (B) Keep file, drop from factory (dead code — same smell). (C) Refactor `ClaudeClassifier` to also use gateway (would be byte-identical to `OpenRouterClassifier` — pointless). |
| 36 | **Pipeline writes the audit row.** `Classifier.classify()` returns `tuple[Classification, LLMResponse]`. Gateway exceptions propagate through classifier to Pipeline; Pipeline catches and writes `record_llm_call` (success) or `record_llm_failure` (gateway error). | Keeps gateway pure compute (no Memory dep); preserves the locked `complete(stage, prompt, response_schema)` surface from #10. `signal_id` naturally available at the call site (Pipeline records it one line before classify). Pipeline is the orchestration layer that knows about all five subsystems — auditing fits there. | (A) Gateway writes audit (breaks #10's surface; `signal_id` would have to flow as a kwarg; couples gateway to Memory). (B) Classifier writes audit (forces classifier to grow Memory + brand_slug deps; pollutes the `Classifier` ABC with `signal_id` param that `FakeClassifier` has to swallow). |
| 37 | **Free function `build_classify_prompt(raw, brand_context) -> str` in `src/resound/prompts/classify.py`.** Concatenates system framing + signal block with `\n\n---\n\n`. Both `Pipeline` (for audit) and `OpenRouterClassifier` (for gateway call) call it. Existing `build_classify_messages` deleted (zero remaining callers post-#35). | Prompt must be available in both success-path `record_llm_call` and failure-path `record_llm_failure` audit rows. Free function shared by both call sites — no drift risk, no awkward ABC param changes, no triple-element tuple returns. Gateway sends `messages=[{role:user, content:prompt}]` per `openrouter.py:225`, so single-string is the correct shape. | (B) Pipeline builds and passes prompt to classifier as param (breaks `Classifier` ABC's `(raw, brand_context)` shape; weakens what classifier means architecturally). (C) Classifier returns prompt as third tuple element (failure path requires custom exception class to surface prompt). |
| 38 | **Named sentinel `JSON_MODE: dict = {}`** added to `src/resound/gateway/__init__.py` public surface (and `__all__`). All call sites pass `response_schema=JSON_MODE`. | Per #2, the gateway uses `response_schema` only as a presence flag. A bare `{}` looks accidental to a code reader; an ad-hoc `{"type": "object"}` implies a structural contract that the gateway doesn't enforce. Named sentinel says exactly what the parameter means in the codebase's vocabulary. Future-proofs against the day someone *does* want to plumb a real schema — the rename target is obvious. | `response_schema={}` (looks like a bug); `response_schema={"type": "object"}` (lies about contract). |
| 39 | **Classifier owns the gateway. Pipeline never holds a gateway reference.** Pipeline imports only `LLMGatewayError` (for the `except` clause). `build_classifier(brand_slug: str) -> Classifier` factory wraps `build_gateway(brand_slug)` → `OpenRouterClassifier(gateway)`. `Pipeline.__init__` signature unchanged — existing test fixtures (`FakeClassifier` injection at `tests/test_pipeline.py:40`) need zero migration. | Tasks 4–8 are explicitly out of scope. Today there's exactly one gateway consumer. Plumbing gateway through Pipeline now optimizes for a refactor not on the roadmap, while widening `Pipeline.__init__`'s 6-kwarg surface and forcing test rewrites. If/when Task 5 (router tiebreaker) lands, that PR is the right place to introduce a Pipeline-held gateway. | Pipeline holds gateway and passes to classifier (premature for skip-4-through-8 scope). |
| 40 | **Stub-as-data on classifier-parse failures.** When `_parse()` can't deserialize gateway-returned content into a `Classification` (no JSON, bad JSON, Pydantic validation rejected the dict), classifier returns `(stub_Classification, LLMResponse)` — Pipeline records a *successful* `llm_calls` row (the gateway call DID succeed) with the unparseable content preserved in `response_content`. New free function `make_fallback_classification(reason: str) -> Classification` in `src/resound/classifiers/__init__.py`, called by classifier (parse failures) and Pipeline (gateway-error path). Static `OpenRouterClassifier._fallback()` removed. `_parse()` no longer takes `raw` (it never used it). | Gateway-fail and parse-fail are different failure modes; audit-trail shape should reflect that. A successful row with unparseable content + stub is the most honest record — operator can investigate why the model emitted off-spec output. Failure rows have `response_content=null` per #30; using them for parse errors loses the model's actual output. | (B) New `ClassifierParseError` exception (Pipeline grows second except clause; failure row schema loses `response_content`). (C) Reuse gateway's `LLMGatewayParseError` (semantically wrong — gateway didn't fail; conflates `error_class` column). |
| 41 | **Hard removal of `RESOUND_CLASSIFIER_PROVIDER`, `ANTHROPIC_API_KEY`, `RESOUND_CLASSIFIER_MODEL`.** Delete from `.env.example`, README, and any reading code. Drop `anthropic` from `pyproject.toml`. No compat shim, no startup deprecation warning. `OpenRouterClassifier.__init__` no longer reads `env("RESOUND_CLASSIFIER_MODEL", ...)`; model resolves through `ModelsConfig` per #16. | Pre-launch product, no real users to migrate. #25 already pre-marked the env var as "removed in Task 9." `models.yaml` is strictly more capable (per-stage, per-brand). A compat shim or warning is overhead with no user to serve. | (B) Compat shim mapping `RESOUND_CLASSIFIER_MODEL` to `defaults.classify.model` inside `load_models_config` (invisible behavior; future debugging confusion). (B') Hard removal + startup deprecation log (ceremony for non-existent migration audience). |
| 42 | **Layered exception backstop in `Pipeline.run_once`.** Three `except` clauses around `classifier.classify()`: (a) `LLMGatewayConfigError` / `LLMGatewayAuthError` re-raised — FATAL per #14; (b) `LLMGatewayError` → `record_llm_failure` + stub via `make_fallback_classification` + `stats.errors += 1`; (c) broad `Exception` → `logger.exception` + stub + `stats.errors += 1` (no audit row — no properly-formed `LLMGatewayError` to record). Stub flows through router → memory → feedback like a normal classification (stub's `is_about_brand=False` / `action_class=IGNORE` triggers the existing `matched_rule="ignored_by_classifier"` branch, mapping to `stats.ignored`). `stats.classified` increments only on the success path. The current `continue` skip on classifier failure (`pipeline.py:81-83`) is removed. | Matches stub-as-data philosophy from #40 — failures become data, never disappear. Demo-resilient: a single weird signal doesn't crash mid-recording (PRD-demo §7 names "obvious failures on camera" as a primary risk). Architecturally honest: every polled signal ends up with rows in `signals`, `classifications`, `routes` — no forest of NULL FKs in the schema. Fatal config/auth still crash the worker so misconfiguration is loud. | (A) Narrow `LLMGatewayError`-only catch (single unforeseen exception crashes recording mid-take). (C) Same as B but keep current `continue` (failures invisible in dashboard — bad for the demo's "every signal accumulates" memory beat). |
| 43 | **Test surface: two-file split + one smoke test.** New `tests/test_classifier.py` (~8 cases): `OpenRouterClassifier` with a `FakeGateway` covering happy path, JSON_MODE sentinel, classify stage name, parse failures (no JSON, bad JSON, Pydantic validation), gateway exception propagation, plus one shape test for `make_fallback_classification`. Extended `tests/test_pipeline.py` (~9 new cases): updated `FakeClassifier` returning tuple; audit on success; audit on `LLMGatewayError`; fatal Config/Auth propagation; broad `Exception` → stub no-audit; stub routes-as-ignored; `stats.classified` only on success; multi-signal isolation. One end-to-end smoke test wiring real `OpenRouterClassifier` + `FakeGateway` + real `Pipeline` + real `SqlMemory`, asserting `llm_calls` row populates with correct fields. ~17 new tests, ~300 lines. | Standard layering — classifier-with-mock-gateway and Pipeline-with-mock-classifier. Each layer's contract testable in isolation. Smoke test catches wiring bugs that pure mocks don't reach (audit-row schema fields actually populating end-to-end), and is the closest test analogue to `resound poll-once` — the demo's invocation. | (A) Single-file extension of `test_pipeline.py` (Pipeline tests have to mock `OpenRouterGateway`; wide setup for narrow assertions). (C) Maximal full integration suite (redundant with the smoke test alone). |
| 44 | **`cli.py` healthcheck shows brand-override status for the classify model.** Resolve via `load_models_config(brand).get_stage_config("classify")`; compare against `load_models_config(None).get_stage_config("classify").model` to detect override. Output: model name, source label (`brand override (brands/<slug>/models.yaml)` vs `config/models.yaml (global default)`), fallback chain, timeout. `OPENROUTER_API_KEY` presence check stays; `RESOUND_CLASSIFIER_PROVIDER` / `RESOUND_CLASSIFIER_MODEL` references removed. | Healthcheck is the green-light gate before recording AND a reviewer's first sanity check after `pip install`. Showing whether the brand override actually took effect is exactly the diagnostic that prevents "I edited models.yaml but nothing changed" confusion. Demo Beat 3 (the money shot) relies on `cp brands/liquiddeath brands/<new>; edit models.yaml; healthcheck still green` — the override-source label visibly verifies the field-level merge from #16 worked. | (A) Minimal output (model name + API key check) — misses the override diagnostic. (B) Verbose all-stages output — three of four stages aren't wired in this scope; would be misleading. |

---

## Task 16 — Ridge Brand Configuration Bundle

Locked in grilling session on **2026-05-07**. Source task: `task-master show 16`.

**Context:** Task 16 creates `brands/ridge/` as the second working brand bundle alongside Liquid Death. The bundle is pure configuration — no code changes — and demonstrates the "onboarding is configuration, not engineering" claim. Together with Liquid Death's bundle, Ridge completes the field-level merge demonstration from Task 1 #16 (Liquid Death overrides; Ridge inherits — see #53). Task 16 has no code dependencies; works pre- and post-Task 9, though Task 9's smart healthcheck (#44) makes #53's narrative fully observable.

| # | Decision | Rationale | Rejected alternatives |
|---|---|---|---|
| 45 | **Brand identity = Ridge** (wallets, rings, knives, EDC accessories). PRD-demo §9 default; publicly confirmed Fulfil customer; active Reddit footprint across `r/ridge`, `r/EDC`, `r/wallets`. | Of PRD-demo §9's named alternatives (Ridge, HexClad, Hims & Hers, Wholly Veggie), Ridge is the only one hitting all four criteria: (a) Reddit volume guaranteeing fresh signals on demo day; (b) clean taxonomy mapping cleanly to standard Resound 7 areas; (c) zero moderation risk; (d) category visually distinct from Liquid Death so the "any D2C brand" claim lands. | HexClad — small subreddit footprint risks zero fresh signals on demo day. Hims & Hers — health/wellness moderation risk surfaces mid-recording; medical signals don't fit standard taxonomy cleanly. Wholly Veggie — smallest brand; near-zero subreddit footprint. Personal-connection brand — would beat Ridge if available, but PRD default stands without one. |
| 46 | **Mirror with localized vocabulary.** Ridge bundle structurally mirrors Liquid Death's: same 8-rule routing shape; people/views are near-copies; brand-specific content concentrates in `understanding.md`, `brand.yaml`, `sources.yaml`. Where rule names would feel cookie-cutter (`cs_damaged_shipment`, `ops_retail_availability`), they're renamed to fit Ridge realities. | Pure mirror leaves leaky names that read as theater (`ops_retail_availability` for a DTC-heavy brand). Pure divergence requires fabricating Ridge org structure (e.g. invented owner identities like `@hardware-lead`) — authenticity-via-fabrication backfires with informed reviewers. Mirror with localized vocabulary preserves the "structure stays, brand context swaps" narrative while making brand-specific work cluster in `understanding.md` (the file that drives classification quality). | Pure mirror — cookie-cutter smell with leaky names. Pure divergence — authenticity-via-fabrication trap; high effort without proportional payoff. |
| 47 | **`sources.yaml` content.** Reddit only. Subreddits: `ridge`, `EDC`, `wallets`. Search terms: `"ridge wallet"`, `"the ridge"`, `"ridge ring"`. `limit: 25`. G2 + Twitter both `enabled: false`. | `r/EDC` (~1.2M) provides volume insurance — guarantees fresh posts daily. Quote-bounded search terms disambiguate from common-English "ridge" (geological/anatomical/architectural). `"the ridge"` specifically catches highest-density brand mentions because Ridge brands its flagship product as "The Ridge". `r/wallets` adds density bonus from comparison threads. G2 inappropriate for consumer brand; Twitter requires paid tier per existing source-coverage posture. | Brand-only (`r/ridge` alone) — signal-volume risk on slow days. Aggressive volume (broad subs + bare `"ridge"` term) — geological/software false positives; understanding.md needs more guard-rails. |
| 48 | **`understanding.md` matches Liquid Death's depth** (~60 lines, 6 sections: What we sell / Voice & positioning / Functional area taxonomy / Glossary / Examples / Severity guidance). 4 examples covering one each of: route, FYI, ignore, false-positive. | Visual parity between bundles signals equal effort per brand. The 4-example outcome-coverage pattern teaches each `action_class` value at the boundary; dropping to 3 examples leaves one outcome class (typically false-positive) unguided, which is highest-risk for a brand whose name collides with common English. Going deeper has diminishing returns at demo scale; going shallower invites visible asymmetry between bundles. | Shallower (~30 lines) — visible asymmetry; misses one outcome class in examples. Deeper (~90 lines) — diminishing returns; longer prompts cost more per call. |
| 49 | **Section 3 taxonomy: standard 7 areas with Ridge-realistic subareas.** Load-bearing Ridge-distinguishing subareas: `cs > warranty_claims`, `cs > ring_returns`, `product > card_ejection_mechanism`. Includes `billing > Ridge_subscription_billing` (Ridge+); `marketing > YouTube_creator_partnerships`. `ops` light (mostly DTC). All subarea names use snake_case for filter consistency (per #56). | Standard 7 areas keep routing rules cross-bundle comparable. Ridge-specific subareas reflect verifiable patterns (warranty claims from Ridge's lifetime guarantee; card ejection from Ridge's distinctive mechanism; ring sizing from any ring company). `subarea` is `Optional[str]` in `Classification` — guidance not enforced; classifier uses these names when matched. | Conservative 1:1 mapping with Liquid Death's subareas — misses Ridge's distinctive volume drivers. Ambitious custom areas (top-level `safety` or `subscription`) — breaks standard 7-area cross-bundle comparability; premature for low-volume signals. |
| 50 | **Section 4 glossary: 4 entries** — `"The Ridge"` (capitalized; geological disambiguation), `"Basecamp"` (knife product line — distinct subarea routing), `"Buy it for life" / "lifetime guarantee"` (warranty-claim trigger), `"Ridge+"` (subscription distinct from billing). Each entry includes "treat as X" classifier-facing guidance, not just a definition. | `"The Ridge"` is highest-leverage entry — Ridge brand naming literally collides with common English in the same subreddit context (`r/EDC` includes hiking/outdoor content). `"Lifetime guarantee"` gloss does what subarea enumeration alone can't — teaches *trigger language*, not just bucket name. `"Basecamp"` and `"Ridge+"` cleanly separate product lines and billing types. `EDC` and `RFID` skipped intentionally; instruction-tuned models handle public-vocabulary terms without explicit guidance, and prompt-token cost doesn't justify their inclusion. | Expand to 6-8 terms — diminishing returns on prompt-token cost. Minimum 2-3 terms — drops `"Lifetime guarantee"` which is the largest leverage point for warranty subarea routing. |
| 51 | **Section 5 examples: 4 specific signals.** (1) Card ejection mechanism complaint at 6 months → `area=product, subarea=card_ejection_mechanism, severity=medium, action_class=sprint, sentiment=negative`. (2) Warranty claim invoking lifetime guarantee → `area=cs, subarea=warranty_claims, severity=medium, action_class=sprint, sentiment=neutral`. (3) 5-year EDC fandom positive → `area=marketing, severity=low, action_class=fyi, sentiment=positive`. (4) `"the ridge on my Bellroy wallet"` competitive comparison → `is_about_brand=false, action_class=ignore`. Examples 2 and 4 carry explicit `Note:` lines cross-referencing glossary entries that drive the classification. | Each example teaches one `action_class` boundary. Example 2 is where sections 3 (warranty subarea) + section 4 (lifetime guarantee glossary) cash out — shows integration in action. Example 4 tests glossary entry #1's hardest claim (lowercase `"the ridge"` can be off-brand) using the false-positive pattern most likely to actually appear in `r/wallets` comparison threads. The `Note:` lines make the file self-documenting for both the classifier and human readers. | Drop slot 2 (warranty) for generic "wrong size ring" CS example — wastes the highest-leverage example slot. Add 5th low-confidence example — asymmetric with Liquid Death; router handles low-confidence regardless of brand context. |
| 52 | **Sections 1, 2, 6 of `understanding.md` + `brand.yaml`.** Section 1 (What we sell): wallets/rings/knives/EDC; DTC-first; lifetime warranty as load-bearing brand claim. Section 2 (Voice & positioning): minimalist "buy once, cry once"; customer base skews male/EDC-enthusiast; **includes "durability emotional" severity calibration hint** ("Ridge customers take product failure personally — a wallet failure carries weight closer to a software service data loss than a snack tasting weird"). Section 6 (Severity guidance): 4-tier mirror of Liquid Death; critical triggers swapped to knife-product safety incidents, mass warranty failure clusters, counterfeit-product mass exposure. `brand.yaml` description carries `"lifetime warranty as load-bearing brand claim"` tail. | Section 2's "durability emotional" hint is what sections 1+2 must do classifier-side — calibrates severity for failure complaints to match Ridge customers' actual reaction patterns. `brand.yaml` description gets passed to classifier as part of `brand_context` (per `BrandConfig.description`), so the lifetime-warranty tail reinforces section 2 via a different surface (CLI healthcheck output, dashboard header). | Drop section 2's emotional hint — loses severity calibration cue; section 2 reads as filler. Drop severity tier names — breaks parity with Liquid Death; less actionable for the classifier (which uses tier names verbatim per `Severity` enum). |
| 53 | **No `brands/ridge/models.yaml` file. Just absence.** Ridge inherits global defaults entirely. | Together with Liquid Death's override (#22), completes the field-level merge demonstration: Liquid Death proves "you can override any field"; Ridge proves "you can also choose not to." Healthcheck (post-Task 9 #44) displays `source: config/models.yaml (global default)` for Ridge — the system correctly resolved against global defaults. Aligns with #23's "opt-in via absence" principle. Honest: I don't know whether Ridge customers' signals classify better with a stronger model; inventing a justification (the way #22 did for Liquid Death's loud/sarcastic voice) would be theater. | Have a `models.yaml` override with a fabricated brand-specific justification — theater; no evidentiary basis like Liquid Death's "loud/sarcastic voice misreads as genuine/outage-level." Empty `models.yaml` file with comment — worst of both worlds; no-op file requires comment to read. |
| 54 | **`routing.yaml`: 8 rules.** Two renames: `cs_damaged_shipment` → `cs_warranty_or_damage`; `ops_retail_availability` → `ops_distribution_gripes`. One swap: `product_roadmap_input` → `product_durability_complaint` (`when: area=product, severity>=medium → @product-lead`). Three owner identity renames cascade to `people.yaml`: `#exec-pr` → `#exec`, `@retail-ops` → `@distribution-lead`, `@product-pm` → `@product-lead`. `notes:` fields on renamed/swapped rules cross-reference Liquid Death's analogues. | Volume math — Ridge generates high-volume durability complaints (most of the EDC signal stream); roadmap input is low-volume in `r/EDC`/`r/wallets`. Without the durability rule, medium-severity product complaints fall to `default_route: "#triage"` — defeats the automated routing pitch. Demonstrates that brand bundles tune routing intelligently: Liquid Death prioritizes roadmap input (canned-water customers suggest flavors); Ridge prioritizes durability (wallets fail and customers expect lifetime warranty). Both correct for their brand. | 9 rules with `product_durability_complaint` ADDED instead of swapped — asymmetric with Liquid Death's 8-rule structure. 8 rules with renames only, no durability rule — highest-volume Ridge signal type gets worst routing treatment (architecturally wrong). |
| 55 | **`people.yaml` + `views.yaml` content.** `people.yaml`: 3 people (`@billing-lead`, `@distribution-lead`, `@product-lead`) and 6 channels (`#triage`, `#exec`, `#cs-team`, `#eng-bugs`, `#marketing-watch`, `#review-queue`) with Ridge-specific descriptions (channel descriptions reference lifetime warranty in `#cs-team`, counterfeit incidents in `#exec`). Header comment acknowledges placeholder identities. `views.yaml`: 3 saved views ("Critical this week", "Warranty escalations", "Durability complaints"); alert thresholds match Liquid Death's numerics (`critical_per_day: 3`; `area_spike_factor: 3.0`). | Channel descriptions reinforce Ridge-specific routing context without inventing fictional team structures. Saved view filter strings depend on classifier emitting matching `subarea` strings — drives #56's snake_case normalization requirement. Alert threshold parity is honest: tuning should be based on observed baseline rates post-deployment, not fabricated demo-time differentiation. | 4th saved view for card-ejection complaints — asymmetric with Liquid Death's 3-view bundle. Diverging alert thresholds — fabricated tuning without measurement. |
| 56 | **Three-layer verification + subarea snake_case normalization.** Layer 1 — Static: files exist + parse + `understanding.md` > 500 chars + no `# TODO` / `# FIXME` outside `people.yaml` header. Layer 2 — Healthcheck: `resound healthcheck --brand ridge` exits 0 + reports correct counts (8 rules, 3 people, 6 channels) + classify model resolves to global default. Layer 3 — Functional: `resound poll-once --brand ridge` ingests + classifies + routes ≥1 real Reddit signal. Subarea strings in sections 3 + 5 of `understanding.md` normalized to snake_case throughout (`warranty_claims`, `card_ejection_mechanism`, etc.) for exact-match consistency with `views.yaml` filter strings. | Functional run is 30 seconds and catches real bugs that healthcheck misses (search terms returning zero, saved-view filter mismatches). Saved-view filter exact-string match risk is real — classifier emits free-form `subarea` strings; section 3 + section 5 + `views.yaml` must agree exactly. PRD-demo §7 names "obvious failures on camera" as primary risk; functional gate prevents. snake_case-everywhere keeps the filter-match contract local and visible. | Layers 1+2 only (defer functional) — defers cheapest test that catches real bugs (saved-view filter mismatch surfaces ONLY in functional runs). Layer 1 only — reckless; bundles can be syntactically valid AND functionally broken (e.g. routing references undefined channel). |

---

## Cross-task implications

Decisions here that affect other tasks — keep in sync when those tasks land:

- **Task 3 (`llm_calls` audit table)**: ✅ designed (decisions #26-#34). Cross-cutting impact on Task 1: `LLMResponse` gains 2 new fields per #29 (the 2-field amendment lands in Task 3's PR, modifying gateway code Task 1 already shipped).
- **Task 7 (LLM telemetry dashboard)**: consumes `query_llm_costs`, `query_llm_latency`, `query_fallback_rate` from Task 3. Decide there whether time-bucketed queries (sparklines) are needed — Task 3 deliberately did not pre-build them (per #34). If sparklines are needed, add a `query_llm_costs_by_day(brand_slug, since)` method to `SqlMemory` in Task 7's PR.
- **Task 9 (refactor classifiers)**: ✅ designed (decisions #35–#44). Wires the Tasks 1–3 infrastructure into the production path. Cross-cutting: deletes `ClaudeClassifier` + `anthropic` SDK (per #35); deletes 3 env vars from `.env.example` and reading code (per #41); updates `README.md:5,45,49,74,121` to drop Claude/Anthropic references and point at `config/models.yaml`; rewrites `cli.py` healthcheck (per #44). New free function `build_classify_prompt` replaces `build_classify_messages` in `src/resound/prompts/classify.py` (per #37). Adds `JSON_MODE` sentinel to `gateway/__init__.py` public API (per #38). Adds `make_fallback_classification` free function to `classifiers/__init__.py` (per #40). Note: per #39, Pipeline does NOT hold a gateway reference (revises the original Task 1 #20 forecast — gateway hides behind classifier instead). The Pipeline three-tier exception backstop and audit-write pattern from #36/#42 is the canonical reference for any future gateway consumer (Tasks 4/5/6/8 if ever picked up).
- **Task 16 (Ridge brand bundle)**: ✅ designed (decisions #45–#56). No code dependencies — pure configuration bundle. Cross-cutting: provides the **inheritance side** of the field-level merge demonstration (per #53), reinforcing decisions #16 (merge semantics), #22 (Liquid Death override = override side), and #23 (opt-in via absence). Task 9's smart healthcheck (#44) makes #53's narrative fully observable — `resound healthcheck --brand ridge` post-Task-9 will display `source: config/models.yaml (global default)`. Consumed by Task 17 (pre-seed memory runs `poll-once --brand ridge` 2-3 times to populate signals). Task 18 (README polish): the `README.draft.md` produced 2026-05-07 already references `brands/ridge/` as a shipping bundle; becomes accurate when Task 16 lands. Task 25 (Beat 3 — money shot) depends on this bundle existing for the on-camera "two brands working" beat. Task 21 (model swap demo): user dropped beat 4, so Ridge's lack of override doesn't muddy the demo narrative. **Critical for verification (per #56):** subarea strings in `understanding.md` sections 3 + 5 must match `views.yaml` filter strings exactly (snake_case throughout) — saved-view filter mismatch is the failure mode functional verification specifically guards against.
- **Task 10 (CLI model testing)**: must build a one-off gateway with a custom `ModelsConfig` (per #8 — no `model_override` on `complete()`).
- **Task 14 / Task 18 (README updates)**: `README.md:45` documents `RESOUND_CLASSIFIER_MODEL` as the model selection mechanism — this becomes stale after Task 2 lands. Either Task 14 or Task 18 should rewrite that section to point at `config/models.yaml` and `brands/<slug>/models.yaml` as the canonical model selection mechanism.
- **Task 21 (verify demo flow)**: revisit model picks in `config/models.yaml` AND in `brands/liquiddeath/models.yaml` (per #17 and #22) against current top-tier efficient+cheap models on OpenRouter. The single-field override pattern should be preserved — only the model name itself should change.
- **Task 25 (Video Beat 3 — The Money Shot)**: the on-camera artifact is `brands/liquiddeath/models.yaml`. Script should narrate the rationale comment, not just the model swap, so the field-level merge story lands.

---

## Superseded decisions

_None yet. When a decision is replaced, move the original here with a note explaining the new decision and which task changed it._
